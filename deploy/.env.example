# 이 파일을 복사해서 .env 로 만들고 실제 값을 채운다.
#   cp .env.example .env
#
# .env 는 .gitignore 대상이라 커밋되지 않는다.
# 이 파일(.env.example)에는 실제 값을 절대 적지 않는다 — 커밋되기 때문이다.

MYSQL_ROOT_PASSWORD=여기에_긴_임의_문자열
MYSQL_USER=wms
MYSQL_PASSWORD=여기에_다른_긴_임의_문자열
